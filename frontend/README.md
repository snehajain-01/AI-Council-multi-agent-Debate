# AI Council Frontend

React + TypeScript + Vite + Tailwind CSS.

## Setup

```bash
npm install
npm run dev
```

Runs at http://localhost:5173. The backend API must be running separately (see `../backend`) for the app to function.
